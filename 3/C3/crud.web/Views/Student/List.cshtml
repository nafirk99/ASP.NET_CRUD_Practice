@model List<crud.web.Models.Student>

    @{
        ViewData["Title"] = "Student List";
    }

<h1>Student List</h1>

<table class="table">
    <thead>
        <tr>
            <td>Name</td>
            <td>Email</td>
            <td>Subscribed</td>
            <td></td>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Email</td>
                <td>@item.Subscribed</td>
                <td>
                    <a class="btn btn-primary" asp-action="Edit" asp-controller="Student" asp-route-id="@item.Id">Edit</a>
                    <form method="post" asp-controller="Student" asp-action="Delete" asp-route-id="@item.Id" style="display:inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>